# Redisの設定はreloadtex不可
redis:
  host: localhost
  config: 6379
  user: yukitexture
  password: ""

# リソパのURL (reloadtex可)
url: ''

# server1 <-> server2の移動の場合はリソースパックが送信されない (reloadtex可)
dontApplyTexture:
- server1
- server2
